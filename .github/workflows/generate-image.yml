name: 🎨 Generate AI Image
run-name: Generating image - ${{ github.event.inputs.prompt }}

on:
  workflow_dispatch:
    inputs:
      prompt:
        description: 'What image do you want?'
        required: true
        default: 'boy jumping from airplane, cinematic lighting, dramatic sky'
        type: string
      steps:
        description: 'Number of inference steps (4-8 for LCM)'
        required: false
        default: '4'
        type: choice
        options:
          - '4'
          - '6'
          - '8'
      seed:
        description: 'Random seed (leave empty for random)'
        required: false
        default: ''
        type: string

jobs:
  generate-image:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: 📦 Cache Python Packages
        uses: actions/cache@v4
        id: cache-pip
        with:
          path: ~/.cache/pip
          key: pip-packages-v1-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            pip-packages-v1-${{ runner.os }}-
      
      - name: 📦 Cache HuggingFace Models
        uses: actions/cache@v4
        id: cache-hf
        with:
          path: ~/.cache/huggingface
          key: hf-models-v1-${{ hashFiles('**/generate.py') }}
          restore-keys: |
            hf-models-v1-
      
      - name: 📚 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
          pip install --upgrade huggingface_hub
          pip install diffusers transformers accelerate safetensors pillow
      
      - name: 🎯 Generate Image
        env:
          HF_HUB_DISABLE_PROGRESS_BARS: 1
          HF_HUB_ENABLE_HF_TRANSFER: 0
          HF_HUB_OFFLINE: ${{ steps.cache-hf.outputs.cache-hit == 'true' && '1' || '0' }}
        run: |
          python generate.py "${{ github.event.inputs.prompt }}" \
            --steps ${{ github.event.inputs.steps }} \
            ${{ github.event.inputs.seed != '' && format('--seed {0}', github.event.inputs.seed) || '' }}
      
      - name: 📤 Upload Generated Image
        uses: actions/upload-artifact@v4
        with:
          name: generated-image
          path: |
            output.png
            generation-info.txt
          retention-days: 7
      
      - name: 🎉 Show Image Info
        run: |
          echo "✅ Image generated successfully!"
          echo "📝 Prompt: ${{ github.event.inputs.prompt }}"
          echo "⏱️ Check 'generation-info.txt' for timing details"
          ls -lh output.png
